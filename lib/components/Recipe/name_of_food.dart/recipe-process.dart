import 'package:flutter/material.dart';

class RecipeProcess extends StatelessWidget {
  const RecipeProcess({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("Recipe Process Page"),
      ),
    );
  }
}
